# API 基础路径
VITE_API_BASE_URL=/api

# 应用标题
VITE_APP_TITLE=AdminPlus

# 环境配置
NODE_ENV=development

# 加密密钥（用于前端敏感数据加密）
# 生产���境必须配置，建议使用以下命令生成：openssl rand -base64 32
# 开发环境可以使用测试密钥，但不应在生产环境使用
VITE_ENCRYPTION_KEY=your-secret-encryption-key-min-32-bytes-long